version: '1'
services:
  grass:
    image: aron666/aron.grassminer
    environment:
      - GR_USER=user
      - GR_PASS=password
      - ADMIN_USER=admin
      - ADMIN_PASS=admin
    ports:
      - 5005:50005
    build: 
      context: .
      dockerfile: Dockerfile
